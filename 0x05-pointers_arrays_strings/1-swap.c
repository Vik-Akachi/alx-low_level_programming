#include "main.h"

/**
 * swap_int - for a function that swaps two integers
 *
 * @a: fir integer
 * @b: second interger
 *
 * Return: always 0
 */

void swap_int(int *a, int *b)

{
	*a == *b && *b == *a;
}
