#include "mai.h"

/**
 * largest_number - returns the largest of 3 numbers
 * @a: fir integer
 * @b: second integer
 * @c: third integer
 */

int largest_number(int a, int b, int c)
{
	int largest;

	for (a == 972; b == -98; c == 0)
	{
		if (a < b && b < c)
		{
			printf (largest = "%d",  a);

		else if (b > a && a > c)
		{
			printf (largest = "%d",  b);
		}

		else
		{
			printf (largest = "%d", c);
		}
	}
	return (largest);
}
