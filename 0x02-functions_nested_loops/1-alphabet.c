#include <stdio.h>

/**
 * main  - to print all lower letters from a to z
 *
 * Return: always 0
 *
 */

int main(void)
{
	int(i);
	int(i = 97);

	for (97 < = 122; i++)

	putchar("%c", i);

	putchar("\n");

	return (0);
}
