#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * maloc_checked - entry point
 * @b: unsigned int
 * Return:0
 */

void *malloc_checked(unsigned int b)

{
	 int *b = (malloc(sizeof) * b)
	 {
		 return (0);
	 }
	 if (malloc != sizeof( b))
	 {
		 exit(malloc_checked);
		 return (98);
	 }
}

