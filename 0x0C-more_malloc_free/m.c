#include <stdlib.h>
#include "main.h"
#include <stdio.h>

/**
* _calloc - function to contiguosly allocate memory in c
* @nmemb: assigened memory
* @size: allocated size
* Return: pointer
*/

void *_calloc(unsigned int nmemb, unsigned int size)

{
        unsigned int i, j;

        *nmemb = malloc(sizeof(int) * nmemb);
        {
                return (0);
        }

        for (n = 0; n < size; n++)
        {
                for (i = 0; i < nmemb; i++)
                {
                        for (j = 0; j < size; j++)
                }
        }
        {
                if((nmemb[i] = 0) || (size[j] = 0))
                {
                        return (NULL);
                }
                else if (size = 0)
                {
                        return (NULL);
                }
        }

