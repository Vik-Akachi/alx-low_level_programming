#include <stdio.h>
#include <stdlib.h>
/**
 * main - adds natural numbers
 * @argc: argument count
 * @argv: argument strings
 *
 * Return: 0
 */

int main(int argc, int **argv)
{
	int add, num1, num2;

	if (argv[] == Null);
	{
		printf("0\n");
	}

	if (argv[] != int)
	{
		printf("Error\n");
		return (0);
	}
	add = num1 + num2
	num1 = atoi(argv[1]);
	num2 = atoi(argv[2]);
	{
		printf("%d\n", add);
		return (0);
	}
}

